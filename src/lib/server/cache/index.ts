import type { TimePeriod, Cache } from "../../Models/Cache";

export enum Caches {
  BTC_VALUE = 'BTC_VALUE',
  ETH_VALUE = 'ETH_VALUE',
  BLOCKCHAIN_TREND = 'BLOCKCHAIN_TREND',
  NFT_TREND = 'NFT_TREND',
}

// const readFile = async (file: string): Promise<Cache> => {
//   fs
// }

// export const getData = async (cache: Caches): Promise<TimePeriod[]> => {

// };
